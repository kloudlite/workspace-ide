name: Docker

on:
  push:
    branches: [master]
    tags: ["v*"]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Write SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519

      - name: Configure SSH
        run: |
          echo "Host kmac.khost.dev" >> ~/.ssh/config
          echo "  StrictHostKeyChecking no" >> ~/.ssh/config
          echo "  UserKnownHostsFile /dev/null" >> ~/.ssh/config

      - name: Test SSH
        run: ssh karthik@kmac.khost.dev "echo connected"

      - uses: actions/setup-go@v5
        with:
          go-version: "1.22"

      - name: Set up Dagger
        uses: dagger/dagger-for-github@v7
        with:
          version: "latest"

      - name: Build and publish on kmac
        run: |
          TAG="${{ github.ref_name == 'master' && 'latest' || github.ref_name }}"
          DOCKER_HOST=ssh://karthik@kmac.khost.dev dagger call publish \
            --address ghcr.io/kloudlite/workspace-ide:$TAG
        env:
          DAGGER_CLOUD_TOKEN: ${{ secrets.DAGGER_CLOUD_TOKEN }}
